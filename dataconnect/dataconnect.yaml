specVersion: "v1beta"
serviceId: "game-review-project"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "game-review-project-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
